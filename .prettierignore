node_modules
dist
.tsbuild
coverage
playwright-report
test-results
blob-report
bun.lock
.claude
.local

# Migrations are SQL, which Prettier has no parser for; they are generated by
# `bun run db:generate` and applied verbatim.
packages/db/migrations

# Helm templates are Go templates with a .yaml extension: Prettier cannot parse
# them, and reformatting one would change what it renders.
deploy/helm/muster/templates
