node_modules
dist
.tsbuild
coverage
playwright-report
test-results
blob-report
bun.lock
.claude
.local

# Migrations are SQL, which Prettier has no parser for; they are generated by
# `bun run db:generate` and applied verbatim.
packages/db/migrations
