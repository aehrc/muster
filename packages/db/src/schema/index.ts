/**
 * The Drizzle tables, re-exported.
 *
 * `drizzle.config.ts` points `drizzle-kit generate` at this module, so a table that
 * is not reachable from here produces no migration.
 *
 * Empty until the first user story lands its tables.
 *
 * Author: John Grimes
 */

export {};
