{
  "name": "@muster/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Pure Muster domain logic: pairing, statements, tickets, checks, personas, brands, harness, limits",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --build",
    "test": "bun test src/ --timeout 120000"
  },
  "devDependencies": {
    "@types/fhir": "0.0.44"
  }
}
