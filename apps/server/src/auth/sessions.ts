/**
 * Sessions: stub surface.
 *
 * @author John Grimes
 */

/** The cookie the opaque session token travels in. */
export const sessionCookieName = "muster_session";

/** How long a session lives. */
export const sessionLifetimeMs = 14 * 24 * 60 * 60 * 1000;
