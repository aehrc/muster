/**
 * The console's root component.
 *
 * A placeholder, replaced by the router and layout in the foundational phase.
 *
 * Author: John Grimes
 */

/** Renders the console shell. */
export function App() {
  return (
    <main>
      <h1>Muster</h1>
      <p>Connectathon participant directory.</p>
    </main>
  );
}
